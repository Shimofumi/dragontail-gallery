---
import type { MarkdownInstance, Page } from "astro";
import type { GetStaticPaths } from "astro";
import Layout from "../../../../layouts/Layout.astro";
import Pagination from "../../../../components/Pagination.astro";
import GalleryGrid from "../../../../components/GalleryGrid.astro";

export interface Props {
  page: Page;
  posts: MarkdownInstance<Record<string, any>>[];
}

export const getStaticPaths = (({paginate}) => {
  const allPosts : MarkdownInstance<Record<string, any>>[] = Object.values(import.meta.glob("../../../picture/*.md", { eager: true }));
  
  // R18/R18Gタグを持つ作品のみ
  const nsfwPosts = allPosts.filter(post => {
	const tags = post.frontmatter.tags || [];
	return tags.includes("R18") || tags.includes("R18G");
  });
  
  nsfwPosts.sort((a, b) => {
		const aDate = new Date(a.frontmatter.date);
		const bDate = new Date(b.frontmatter.date);
		return bDate.getTime() - aDate.getTime()
	});

  const uniqueTags = [
    ...new Set(nsfwPosts.map((post) => post.frontmatter.tags).flat()),
  ] as string[];

  return uniqueTags.flatMap((tag) => {
    const filteredPosts = nsfwPosts.filter((post) => post.frontmatter.tags.includes(tag));
    return  paginate( filteredPosts , {
      params: { tag },
      pageSize: 18
    });
  });
}) satisfies GetStaticPaths;

const { tag } = Astro.params;
const { page } = Astro.props;
---
{ tag === undefined ? <></> : (
<Layout title={`${tag}がついたページ (NSFW)`} isNSFW={true}>
  <h1><span>{tag}</span>がついたページ - {page.currentPage}</h1>
    <GalleryGrid page={page} />
    <Pagination page={page} path={`/nsfw-gallery/tags/${tag}`} />
</Layout>
)}
